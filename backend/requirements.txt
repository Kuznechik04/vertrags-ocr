fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
email-validator==2.2.0
pydantic-settings==2.5.2
python-multipart==0.0.9
PyJWT==2.9.0
alembic==1.13.3
pdfplumber==0.11.4
pymupdf==1.28.2
Pillow==10.4.0
python-dotenv==1.0.1
pytesseract==0.3.13
easyocr==1.7.2
python-doctr[torch]==1.1.0
openpyxl==3.1.5

# torch wird jetzt auch für den Mock-Backend-Standard (docTR/EasyOCR) benötigt,
# nicht mehr nur fürs Donut-Fine-Tuning.
torch==2.6.0

# ML / Fine-Tuning (nur benötigt, wenn ein echtes Modell trainiert/geladen wird)
transformers==4.48.3
tokenizers==0.21.0
datasets==2.21.0
accelerate==0.34.2
sentencepiece==0.2.1
seqeval==1.2.2

# Optional: nur nötig, wenn statt der Standard-SQLite-DB Postgres genutzt wird
# (DATABASE_URL=postgresql://...). Erfordert eine lokale PostgreSQL-Installation
# (liefert `pg_config`), sonst schlägt der Build fehl.
# psycopg2-binary==2.9.9
